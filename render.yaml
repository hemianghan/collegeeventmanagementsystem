services:
  - type: web
    name: college-event-management-system
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_OPTS
        value: "-Xmx400m -XX:MaxMetaspaceSize=256m"
